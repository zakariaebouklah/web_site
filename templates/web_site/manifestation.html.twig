{% extends 'base.html.twig' %}

{% block title %}Nos Événements{% endblock %}

{% block main %}

    <main class="main">
        <div>
            <img src="{{ asset('images/manif.jpg') }}" alt="manifestation">
        </div>
        <div class="flex flex-row h-96 items-center text-center justify-center centred">
            <a href="{{ path("app_events") }}" target="_blank">
                <div class="w-52 h-60 flex flex-col justify-between mr-14 hover:bg-gray-300 hover:rounded-xl dark:hover:bg-white/30">
                    <div class="items-center text-center">
                        <img src="{{ asset('images/event.png') }}" alt="événements">
                    </div>
                    <div>Consultez Nos Événements</div>
                </div>
            </a>
        </div>
    </main>

{% endblock %}

